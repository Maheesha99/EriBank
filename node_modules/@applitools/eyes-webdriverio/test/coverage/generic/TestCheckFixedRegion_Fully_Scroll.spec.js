const cwd = process.cwd()
const path = require('path')
const assert = require('assert')
const spec = require(path.resolve(cwd, 'src/SpecDriver'))
const {Configuration} = require(cwd)
const {testSetup} = require('@applitools/sdk-shared')

describe('Coverage Tests', () => {
  let driver
  let eyes
  let baselineTestName
  beforeEach(async () => {
    driver = await spec.build(undefined || {browser: 'chrome'})
    eyes = testSetup.getEyes({
      isVisualGrid: undefined,
      isCssStitching: undefined,
      branchName: "master",
    })
  })
  afterEach(async () => {
    await spec.cleanup(driver)
  })
  it('TestCheckFixedRegion_Fully_Scroll', async () => {
    await spec.visit(driver, "http://applitools.github.io/demo/TestPages/fixed-position")
    await eyes.open(
            driver,
            "Eyes Selenium SDK - Fluent API",
            "TestCheckFixedRegion_Fully_Scroll",
            {width: 700, height: 460},
          )
    await eyes.check({region: "#fixed", isFully: true})
    await eyes.close(true)
  })
})