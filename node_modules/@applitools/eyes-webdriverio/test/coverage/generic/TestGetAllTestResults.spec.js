const cwd = process.cwd()
const path = require('path')
const assert = require('assert')
const spec = require(path.resolve(cwd, 'src/SpecDriver'))
const {Configuration} = require(cwd)
const {testSetup} = require('@applitools/sdk-shared')

describe('Coverage Tests', () => {
  let driver, destroyDriver, eyes
  let baselineTestName
  beforeEach(async () => {
    [driver, destroyDriver] = await spec.build(undefined || {browser: 'chrome'})
    eyes = testSetup.getEyes({
      isVisualGrid: undefined,
      isCssStitching: true,
      branchName: "master",
    })
  })
  afterEach(async () => {
    await destroyDriver(driver)
  })
  it('TestGetAllTestResults', async () => {
    await eyes.open(
            driver,
            "Applitools Eyes SDK",
            "TestGetAllTestResults",
            {width: 800, height: 600},
          )
    await assert.rejects(
            async () => {
    const var_3 = await eyes.close(undefined)
    return var_3
    },
            undefined,
            undefined,
          )
    await eyes.getRunner().getAllTestResults(false)
  })
})